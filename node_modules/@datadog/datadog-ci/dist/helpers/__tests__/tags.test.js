"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const tags_1 = require("../tags");
describe('parseTags', () => {
    test('falls back to empty object if invalid format', () => {
        expect(tags_1.parseTags([''])).toEqual({});
        expect(tags_1.parseTags(['not.correct.format'])).toEqual({});
        expect(tags_1.parseTags(['not.correct.format,either'])).toEqual({});
    });
    test('returns an object with the tags with well formatted strings', () => {
        expect(tags_1.parseTags(['key1:value1', 'key2:value2'])).toEqual({ key1: 'value1', key2: 'value2' });
    });
    test('should not include invalid key:value pairs', () => {
        expect(tags_1.parseTags(['key1:value1', 'key2:value2', 'invalidkeyvalue'])).toEqual({ key1: 'value1', key2: 'value2' });
    });
});
//# sourceMappingURL=tags.test.js.map