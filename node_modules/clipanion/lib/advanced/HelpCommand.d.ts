import { RunState } from '../core';
import { BaseContext, CliContext, Cli } from './Cli';
import { Command } from './Command';
export declare class HelpCommand<Context extends BaseContext> extends Command<Context> {
    private readonly realCli;
    private readonly contexts;
    private commands;
    private index?;
    static from<Context extends BaseContext>(state: RunState, realCli: Cli<Context>, contexts: CliContext<Context>[]): HelpCommand<Context>;
    constructor(realCli: Cli<Context>, contexts: CliContext<Context>[]);
    execute(): Promise<void>;
}
